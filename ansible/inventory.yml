all:
  hosts:
    server:
      ansible_host: "{{ lookup('env', 'HOST') }}"
      ansible_user: deploy
      ansible_ssh_private_key_file: /tmp/deploy_key
  vars:
    domain: "terraform.dev-lab.dev"
    certbot_email: "iwan.gubler89@gmail.com"
